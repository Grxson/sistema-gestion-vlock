{
  "version": 2,
  "builds": [
    {
      "src": "desktop/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "desktop/dist"
      }
    }
  ],
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "desktop/dist/index.html" }
  ]
}
